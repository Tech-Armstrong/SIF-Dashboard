{
  "name": "sif-dashboard",
  "private": true,
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "cd backend && python -m uvicorn main:app --reload --port 8010",
    "dev:all": "start /B cmd /c \"npm run dev:backend\" && npm run dev:frontend"
  }
}
